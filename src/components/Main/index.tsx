import './index.scss';

import React from 'react';

const Main: React.FC = () => {
    return (
        <div className="AppMain">
            <div id="main-view" />
        </div>
    );
};

export default Main;
