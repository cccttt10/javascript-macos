import './index.scss';

import React from 'react';

import Header from '../Header';

const App: React.FC = (): JSX.Element => (
    <div className="App">
        <Header />
    </div>
);

export default App;
